1. The running time is n^2, because there are two nested loops.
2. The running time is n*m, because there are two nested loops with different length(n, m).
2. The running time is n*m, there is a loop and it call the method.